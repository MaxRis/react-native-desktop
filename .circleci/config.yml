# Javascript Node CircleCI 2.0 configuration file
version: 2
jobs:
  build:
    docker:
      - image: buildpack-deps:trusty

      # specify the version you desire here
      - image: circleci/node:7.10

    working_directory: ~/repo

    steps:
      - run: echo 'export PATH=/opt/qt55/bin/qmake:$PATH' >> $BASH_ENV

      - checkout

      # Install and configure env with Qt
      - run: |
          sudo apt-get update -y
          sudo apt-get install -y software-properties-common
          sudo add-apt-repository -y ppa:beineri/opt-qt551-trusty
          sudo apt-get -qy update
          sudo apt-get install -y cmake qt55base qt55webkit
          echo "/opt/qt55/bin/qt55-env.sh" >> ~/.circlerc
          
      - run: qmake -version

      # Build react-native-linux
      - run: |
          mkdir build
          pushd build
          cmake .. && make -j4
          popd

      # Download and cache dependencies
      - restore_cache:
          key: dependency-cache-{{ checksum "package.json" }}

      - run: npm install

      - save_cache:
          key: dependency-cache-{{ checksum "package.json" }}
          paths:
            - node_modules
        
      # run tests!
      - run: npm test